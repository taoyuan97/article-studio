# T010：公众号发布（四）E2E 验收、文档与交付

## 1. 任务信息

- 状态：已完成
- 优先级：P0
- 类型：正式任务 10/10
- 前置任务：T007、T008、T009
- 后续任务：无
- 目标目录：`frontend/`（E2E）、`docs/`、根 README
- 创建日期：2026-08-24
- 关联文档：`docs/task/T007`–`T009`、`docs/ops/`（手工任务文档惯例）

## 2. 目标

以 fake 模式完成发布链路 E2E 验收，补齐真实发布的手工操作文档（个人公众号凭据/IP 白名单/wenyan-mcp 安装），同步更新 README 与既有三份文档，完成本阶段交付。

## 3. 范围

### 3.1 必须实现

**Playwright E2E（fake 模式，不依赖真实微信凭据）**

- 新用例 `publish.spec.ts`：
  - 主路径：从文章工作台"发布到公众号"入口进入 → 选文章/版本 → 勾选 2+ 图片并调整位置与封面 → 选主题 → 预览出现组装 Markdown → 发布 → 成功态展示 FAKE media_id → 发布记录列表出现该记录。
  - 校验路径：未选主题时发布按钮不可用；发布失败（后端注入错误）时展示失败文案可重试。
- E2E 环境沿用现有编排（假模型/假服务 + `publish_fake_mode=true`），不触真实网络。
- 注意已知坑（见项目记忆）：E2E 断言用词需与 fake 响应严格一致；必要的状态可见性用延时或显式等待。

**文档交付**

- `docs/ops/manual-tasks-t007-t010.md`（沿用 T005/T006 手工任务文档格式），至少包含：
  1. 个人公众号获取 AppID/AppSecret 步骤（mp.weixin.qq.com → 设置与开发 → 基本配置；未认证个人订阅号可行；IP 白名单位置）。
  2. 本机公网 IP 查询与加入白名单步骤；IP 变动（家宽）后的更新方法。
  3. `npm install -g @wenyan-md/mcp` 安装与 `wenyan-mcp --help` 验证。
  4. `.env` 配置示例（`WECHAT_APP_ID/WECHAT_APP_SECRET/WENYAN_MCP_COMMAND/PUBLISH_FAKE_MODE`）。
  5. 真实发布冒烟清单（6 步左右：选文章→配图→主题→预览→发布→公众号后台草稿箱核对）。
  6. 常见错误对照（40164 IP 不在白名单 / 41004 缺 secret / 超时）。
  7. 验证记录留空待人工填写。
- 根 README：功能表新增"发布到公众号"；`.env` 配置表补充新变量；使用说明新增发布页一节；FAQ 增补发布相关（如 IP 白名单报错）。
- PRD、tech-design、T007–T009 任务文档状态随实施结果更新（"待开始"→"已完成"，勾选验收项）。

**清理与回归**

- `backend/data/publish_tmp/` 临时文件策略确认（保留快照用途或清理规则写入文档）。
- 全量回归：pytest、Vitest、ESLint/tsc/build、既有 E2E 11 用例 + 新增 publish 用例全部通过。

### 3.2 不实现

- 真实 API Key 依赖的自动化用例（保留手工冒烟清单，沿用项目既有原则）。
- Server 模式部署文档（仅在手工文档中提及作为 IP 白名单痛点的后续选项）。

## 4. 测试

- `pnpm e2e`：既有 11 用例 + publish 用例全部通过。
- 全量回归：pytest（51+ 新增）、Vitest（26+ 新增）通过。

## 5. 验收标准

- [x] publish E2E 主路径 + 校验路径全部通过，且全程 fake 模式无真实网络依赖。
- [x] `docs/ops/manual-tasks-t007-t010.md` 覆盖上述 7 项内容。
- [x] 根 README 功能表/配置表/使用说明/FAQ 更新完整。
- [x] PRD、tech-design、T007–T009 状态与验收项同步更新。
- [x] 全量回归（pytest/Vitest/ESLint/tsc/build/E2E）通过。
- [x] 临时文件与日志无残留（或清理规则已文档化）。
- [x] `prototype/` 目录未被改动。

## 6. 完成定义

上述验收全部通过后，T010 完成，公众号发布阶段交付：

- 开发环境（fake）端到端发布流程可演示、可回归；
- 真实发布所需的人工操作全部文档化，用户按手册配置后即可将文章一键发至个人公众号草稿箱。
